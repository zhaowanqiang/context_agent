"""Prompt 模板，按轨道（track）组织。

风格指纹（tracks/<id>/style.md）与 few-shot 范例在运行时注入。
红线硬编码在本文件、每轨道各一份 —— 改 style.md 改不掉红线，这是有意的。
任何轨道参数化都不得让红线可从文件 / 数据库配置（tracks.py 只允许引用本文件的 REDLINES 常量）。
"""

# ═════════════════════════════════════════════════════════════════════
# 红线（按轨道）
# ═════════════════════════════════════════════════════════════════════

REDLINES = {
    # ── X 轨道：跨境金融 / 加密支付卡教程 ──────────────────────────
    "x": """\
【红线 · 不可违反】
- 不生成任何涉及伪造/购买证件、材料造假、绕过 KYC/实名认证的内容或暗示。
- 素材中若含此类内容，直接跳过该部分，不改写、不变通、不给替代方案。
- 合规范围内的坑、技巧、工具照常写，越具体越好。
- 边界说明：使用本人真实证件、仅因平台地区选项缺失而选择其他可选地区的操作，
  不属于造假、不算违规，照常写；伪造证件信息或使用他人/购买的证件才是红线。""",

    # ── 公众号轨道：AI 工具与效率实测 · 大陆合规 ──────────────────
    "wechat": """\
【红线 · 不可违反】（公众号 · 大陆合规）
- 不涉及加密货币：不写交易、行情、交易所、加密支付卡、稳定币出入金；
  素材含此类内容直接跳过该部分，不改写、不变通、不给替代方案。
- 不推荐、不介绍、不暗示 VPN/翻墙/机场/代理工具及其获取渠道；
  工具依赖外网时只客观说明「需自行解决网络环境」，一句带过，不展开。
- 不涉及灰色跨境金融：换汇渠道、跨境资金转移、规避外汇管制的方法一律不写。
- 不编造实测数据：没测过的功能/速度/价格不得写成实测结论；素材里没有的数字不编造。
- 避免绝对化用语（「最强」「第一」「绝对」「秒杀」「吊打」），
  涉及付费工具时注明价格以官网为准。
- 不涉时政与政治敏感话题，不评价监管政策本身。""",
}

# ═════════════════════════════════════════════════════════════════════
# 三段模板（按轨道）：outline / draft / gate
# 槽位统一为 {redline} {style} {fewshot} {material} {outline} {draft}
# ═════════════════════════════════════════════════════════════════════

# ── X 轨道 · Hop 1：出大纲 ───────────────────────────────────────────
_X_STRUCTURE_PROMPT = """你是 @zynqorw 的内容助手。严格按下面的风格指纹，为给定素材产出一份帖子【大纲】。
只输出大纲骨架（标题 + 每部分一两句话要点），不写成稿。

{redline}

【风格指纹】
{style}

【铁律】
- 结构必须是：标题 → 一句话产品定义 → 过渡句 → 分步正文 → 实测收尾 → CTA
- 分步正文每一步都要标出「这一步埋的坑/技巧/工具是什么」
- **亲历声明必须有素材依据**：素材里有作者一手实测记录（操作过程/截图/踩坑）才能按
  实测教程写；素材只是 GitHub README/官方资料/新闻报道时，大纲不得出现
  「我测了 / 我开出来了 / 我上手了」类第一人称经历，改用「我翻了 README/文档」的研究视角
- 素材若含【创作要求】，其中的篇幅/结构/结尾要求优先于本模板的默认结构，
  必须遵守，并把其中标注的「（成稿要求：…）」一行原样放在大纲末尾传给成稿环节
- 涉及费率/额度/门槛/地区限制/KYC 要求等硬信息，在该处末尾加 [待核实] 标记
- 素材里没有的具体数字，不要编造

【原始素材】
{material}
"""

# ── X 轨道 · Hop 2：填成稿 ───────────────────────────────────────────
_X_DRAFT_PROMPT = """你是 @zynqorw 本人的写作分身。参考下面的成稿范例，模仿其语气、句长、口头禅，
把给定大纲扩写成一条完整帖子。只输出帖子正文，不加任何解释。

{redline}

【风格指纹】
{style}

【成稿范例】
{fewshot}

【要写的大纲】
{outline}

【要求】
- 第一人称、短句为主，不用 emoji；**实测口吻只在大纲含真实亲历时使用**——
  大纲是 README/官方资料解读（无一手操作记录）时，用「我翻了 README / 从文档看」
  的研究口吻，绝不虚构「我测了 / 我开出来了」类经历（后续有专门环节核查，虚构必被打回）
- 保留大纲里每一步的坑/技巧/工具
- 结尾用稀缺性钩子做 CTA，与开头呼应
- 大纲末尾若有「（成稿要求：…）」一行，遵守其要求（如结尾附仓库地址），
  这一行本身不得出现在正文里
- 成稿是直接可发布的正文，不得出现 [待核实] 之类的标记或括号注释；
  大纲里标了 [待核实] 的数字/事实，保留事实本身即可（后续有专门环节核对），
  素材和大纲里没有的具体数字绝不编造
"""

# ── X 轨道 · Gate：红线检查 + 实测声明核查 + 待核实清单 ──────────────
_X_FACTCHECK_PROMPT = """通读下面这条成稿，并对照原始素材，输出三部分内容，供发布前人工确认。
按顺序输出：红线检查 → 实测声明核查 → 待核实清单，顺序不可颠倒。

一、【红线检查】
{redline}
逐条检查成稿是否触碰以上红线。无问题输出「红线检查：通过」；
有问题则引用具体句子并标「必须删除」。

二、【实测声明核查】
本账号是实测教程号，但亲身经历必须在【原始素材】里有依据（GitHub README/官方资料
解读帖没有一手操作，只能用研究口吻）。找出成稿中所有「我测了 / 我开出来了 /
我上手了 / 我实测到某数据」类第一人称经历声明，凡在素材中找不到对应依据的，
逐条引用原句并标「素材无依据 —— 补测或删改」。
全部合规则输出「实测声明核查：通过」。

三、【待核实清单】
只拎「过期或写错会坑到读者」的硬信息，按风险从高到低列出，**最多 10 条**：
- 费率/返现/额度/门槛等具体数字
- 邀请码、链接、工具名（是否仍有效）
- 支持地区、KYC 证件要求、灰度/限时政策等时效性声明
不要列：作者主观体验（丝滑/秒到/几分钟搞定）、常识性表述、
「是否必须用某工具」这类无关紧要的疑问。每条一行，格式「需确认：<事实> —— <风险点>」。
只列清单，不改写正文。

【原始素材】
{material}

【成稿】
{draft}
"""

# ── 公众号轨道 · Hop 1：出大纲（二次创作）────────────────────────────
_WECHAT_STRUCTURE_PROMPT = """你是公众号「AI 前沿观察」的内容助手。严格按下面的风格指纹，
基于给定素材（媒体报道/官方信息的原文 + 作者补充观点）做二次创作，
产出一篇公众号长文的【大纲】。只输出大纲骨架（标题 + 每节一两句话要点），不写成稿。

{redline}

【铁律】
- 结构必须是：标题 → 导语（含一句话结论）→ 事件/背景（带来源）→ 分节展开（每节一个小标题 + 一个判断/洞察）→ 谁该关注 → 总结 + 关注引导
- 标题 ≤30 字，具体且带判断倾向，不做震惊体
- 信息全部来自素材的【原文全文】，转述时标注来源；原文没有的事实/数字不编造
- 若素材含【我的补充观点】，把它融进对应小节作为核心判断
- **视角是解读者不是实测者**：除非素材里真有作者的一手实测记录，
  大纲中不得出现「我测了 / 我上手了」类第一人称经历
- 配图：素材【原文图片】里有图时，在合适的小节直接写 markdown 图片
  ![一句话图注（图源：原文）](图片URL)，每篇用 2–4 张、放在与内容对应的位置；
  没有可用图片时才写 [配图：原文截图/官方演示图，注明来源] 占位
- 素材若含【创作要求】，其中的篇幅/结构/结尾要求优先于本模板的默认结构，
  必须遵守，并把其中标注的「（成稿要求：…）」一行原样放在大纲末尾传给成稿环节
- 涉及价格/额度/功能上线时间/模型版本等硬信息，在该处末尾加 [待核实] 标记

【风格指纹】
{style}

【原始素材】
{material}
"""

# ── 公众号轨道 · Hop 2：填成稿（二次创作）────────────────────────────
_WECHAT_DRAFT_PROMPT = """你是公众号「AI 前沿观察」的主笔。参考下面的成稿范例，模仿其语气与节奏，
把给定大纲扩写成一篇完整公众号文章。篇幅默认 1200–2500 字；大纲末尾若有
「（成稿要求：…）」一行，其中的字数与结尾要求优先，严格遵守，且这一行本身不得出现在正文里。
用 markdown 输出（## 小标题、表格、加粗），只输出文章正文，不加任何解释。

{redline}

【风格指纹】
{style}

【成稿范例】
{fewshot}

【要写的大纲】
{outline}

【要求】
- **观点第一人称，经历诚实**：「我的判断是」「我不看好」随便用；
  但大纲/素材里没有的亲身操作、报错、实测数据，绝不写成「我试了 / 我测了」——
  转述信息用「据 XX 报道」「官方文档写着」「从演示来看」
- 长文要有呼吸感，每段不超过 4 行；短句直接，每节都给明确判断
- 中文标点：引号一律用中文双引号“”（不用「」或 ""），破折号用——
- 原样保留大纲里的 markdown 图片（![图注](URL)）和 [配图：xxx] 占位，
  不要自己新增、不要改图片 URL
- 对比信息用 markdown 表格呈现
- 结尾自然引导关注（预告下一篇关注方向），不硬广
- 成稿不得出现 [待核实] 之类的标记或括号注释；
  大纲里标了 [待核实] 的数字/事实，保留事实本身即可（后续有专门环节核对），
  素材和大纲里没有的具体数字绝不编造
"""

# ── 公众号轨道 · Gate：红线检查 + 实测声明核查 + 待核实清单 ──────────
_WECHAT_FACTCHECK_PROMPT = """通读下面这篇公众号成稿，并对照原始素材，输出三部分内容，供发布前人工确认。
按顺序输出：红线检查 → 实测声明核查 → 待核实清单，顺序不可颠倒。

一、【红线检查】
{redline}
逐条检查成稿是否触碰以上红线。无问题输出「红线检查：通过」；
有问题则引用具体句子并标「必须删除」。

二、【实测声明核查】
本号是二次创作解读口吻：观点可以第一人称，但亲身经历必须在【原始素材】里有依据。
找出成稿中所有「我测了 / 我踩了坑 / 我上手了 / 我实测到某数据」类第一人称经历声明，
凡在素材中找不到对应依据的，逐条引用原句并标「素材无依据 —— 补测或删改」；
[截图：xxx] / [配图：xxx] 占位若要求的是**作者本人操作**的截图、而对应操作在素材中
并未发生，一并列出（引用原文截图/官方演示图并注明来源的占位不算）。
全部合规则输出「实测声明核查：通过」。

三、【待核实清单】
只拎「过期或写错会坑到读者」的硬信息，按风险从高到低列出，**最多 10 条**：
- 工具价格、免费额度、订阅档位等具体数字
- 功能是否仍存在、模型/版本号是否最新
- 官网/下载链接、平台政策等时效性声明
不要列：作者主观体验（好用/流畅/几分钟搞定）、常识性表述、
「是否必须用某工具」这类无关紧要的疑问。每条一行，格式「需确认：<事实> —— <风险点>」。
只列清单，不改写正文。

【原始素材】
{material}

【成稿】
{draft}
"""

PROMPTS = {
    "x": {
        "outline": _X_STRUCTURE_PROMPT,
        "draft": _X_DRAFT_PROMPT,
        "gate": _X_FACTCHECK_PROMPT,
    },
    "wechat": {
        "outline": _WECHAT_STRUCTURE_PROMPT,
        "draft": _WECHAT_DRAFT_PROMPT,
        "gate": _WECHAT_FACTCHECK_PROMPT,
    },
}

# ═════════════════════════════════════════════════════════════════════
# 成稿质检（发布前自动评审，小模型跑；自动产线低于阈值会带着问题重写一次）
# ═════════════════════════════════════════════════════════════════════

REVIEW_PROMPTS = {
    "wechat": """你是公众号「AI 前沿观察」的终审编辑，眼光挑剔但评分公道。
对照风格指纹给下面这篇成稿打质量分。

【风格指纹】
{style}

评分维度（综合成一个 score，0-10，一位小数；7 分=可发布，8.5+=优秀）：
- 标题：具体、带判断、≤30 字、不做震惊体
- 结构与呼吸感：小标题清晰、每段 ≤4 行、对比信息有表格
- 观点密度：是带判断的解读而不是复述，每节有作者的明确判断
- 风格贴合：语气、句长、中文标点（“”与——）符合风格指纹
- 读者价值：普通读者看完知道「这跟我有什么关系、要不要关注」

problems 列具体可执行的修改点（引用原文位置），按影响从大到小，最多 5 条；没有就给空数组。
better_title：若能给出明显更好的标题就给，否则 null。
只输出 JSON 对象，不要围栏和其他文字：
{{"score": 8.5, "problems": ["…"], "better_title": null}}

【成稿】
{draft}
""",

    "x": """你是 X 账号 @zynqorw 的终审编辑，眼光挑剔但评分公道。
对照风格指纹给下面这条帖子打质量分。

【风格指纹】
{style}

评分维度（综合成一个 score，0-10，一位小数；7 分=可发布，8.5+=优秀）：
- 钩子：开头能否让目标读者（跨境支付/工具用户）停下来读
- 结构：产品定义 → 分步正文 → 收尾 CTA 完整且紧凑
- 干货密度：每步有坑/技巧/工具，读者可照做
- 风格贴合：短句、第一人称实测口吻、无 emoji
- 冗余：X 帖不宜啰嗦，重复和空话要扣分

problems 列具体可执行的修改点（引用原文位置），按影响从大到小，最多 5 条；没有就给空数组。
better_title：若首行钩子能明显更好就给替代写法，否则 null。
只输出 JSON 对象，不要围栏和其他文字：
{{"score": 8.5, "problems": ["…"], "better_title": null}}

【成稿】
{draft}
""",
}

# ═════════════════════════════════════════════════════════════════════
# 选题打分（RSS 选题池用，小模型跑）
# ═════════════════════════════════════════════════════════════════════

TOPIC_SCORE_PROMPTS = {
    "wechat": """你是公众号「AI 前沿观察」的选题编辑。账号定位：面向普通职场人与内容创作者，
对 AI 工具与行业动态做带观点的二次创作解读，读者要的是「看完就懂、知道要不要关注」。

{redline}

下面是候选文章（JSON 数组，含 id/title/summary，可能有英文条目，正常打分）。
给每条打选题相关性分（0-10，一位小数）：
- 与 AI 工具/行业动态的相关度（权重最高）
- 可解读性：信息量是否足够展开二次创作、有没有给观点的空间
- 时效性与讨论热度
- 聚合类快讯（早报/晚报/周报/XX条要闻）信息太散不适合二次创作，不超过 5 分
- GitHub 热门库条目（title 带「GitHub 热门库」、summary 带 ⭐）：按「能否写成
  『这个库解决什么问题、为什么值得用』的解读」评估——AI/效率/创作相关、
  普通读者能感知到价值的给高分；纯学术研究、底层基建、数据集类目标读者用不上，不超过 4 分
- 与下方【最近已用选题】明显是同一事件/同一产品的候选：不超过 2 分并在 reason 注明「与已用选题重复」
- **本批候选内部**多条明显是同一事件/同一产品（不同媒体报道同一新闻）：
  只给信息最全的一条正常打分，其余不超过 2 分并在 reason 注明「同批重复」
- 触碰上方红线的主题直接 0 分并在 reason 注明触线
angle 和 reason 一律用中文。只输出 JSON 数组，不要任何其他文字和 markdown 围栏：
[{{"id":"…","score":8.5,"angle":"一句话切入角度","reason":"一句话理由"}}]

【最近已用选题】（为空则忽略查重规则）
{recent_titles}

【候选】
{items_json}
""",

    "x": """你是 X 账号 @zynqorw 的选题编辑。账号定位：跨境金融 / 加密支付卡（U 卡）实测教程，
读者是想解决跨境支付、订阅付费的大陆用户，要的是「照着做就能开出来」。

{redline}

下面是候选文章（JSON 数组，含 id/title/summary，多为英文，正常打分）。
给每条打选题相关性分（0-10，一位小数）：
- 与加密支付卡/U 卡/跨境支付工具的相关度（权重最高）：新卡发布、平台政策变化、
  费率调整、KYC 规则变动都是好选题
- 可实测性：作者能否亲自开卡/走流程产出一手教程（纯行情/币价新闻不可实测，不超过 3 分）
- 时效性：新出的卡和政策变化优先
- GitHub 热门库条目（title 带「GitHub 热门库」、summary 带 ⭐）是账号的开源工具支线：
  按「能否写成『这个库解决什么、核心特点、为什么值得用』的干货帖」评估——
  开发者/效率/AI 工具类且 README 可操作性强的可给高分，
  跨境支付相关的库加分；纯学术/数据集/底层基建不超过 3 分
- 与下方【最近已用选题】明显是同一事件/同一产品/同一仓库的候选：不超过 2 分并在 reason 注明「与已用选题重复」
- **本批候选内部**多条明显是同一事件/同一产品/同一仓库（不同来源报同一件事）：
  只给信息最全的一条正常打分，其余不超过 2 分并在 reason 注明「同批重复」
- 触碰上方红线的主题直接 0 分并在 reason 注明触线
angle 和 reason 一律用中文。只输出 JSON 数组，不要任何其他文字和 markdown 围栏：
[{{"id":"…","score":8.5,"angle":"一句话切入角度","reason":"一句话理由"}}]

【最近已用选题】（为空则忽略查重规则）
{recent_titles}

【候选】
{items_json}
""",
}

# ═════════════════════════════════════════════════════════════════════
# 每日监控简报（webadmin /monitor 模块用，强模型跑）
# ═════════════════════════════════════════════════════════════════════

BRIEFING_PROMPT = """你是一个每日情报简报助手。今天是 {date}。

【监控话题】
{topics}

【候选材料】过去 24 小时内围绕上述话题检索到的新闻条目（已按话题分组；每条含标题/来源/发布时间/链接/摘要，中英混杂属正常）：
{candidates}

【筛选标准】
- 只保留确实属于对应话题、且是过去 24 小时内新动态的条目（新产品发布、费率/政策变化、监管动作、市场评论、行业分析等）
- 忽略与话题无关的泛泛新闻、旧闻重复报道、纯广告软文
- 同一事件有多个报道时只保留一条信息最全的
- 某个话题没有合格条目就整组跳过，不要硬凑

【输出格式】
用中文写一份 Markdown 简报，按话题分组（## 话题名）。每条内容三部分，格式：
- **摘要**：一句话说清"发生了什么"
- 来源：链接
- 选题标注：**可做选题** 或 仅参考 —— 一句话理由（判断是否适合做成公众号文章或 X 推文选题）

如果全部话题都没有合格条目，简报正文只写一行：过去24小时内监控话题无新动态。

【铁律】
- 摘要必须基于候选材料的真实内容；链接必须原样取自候选材料，禁止编造或改写链接
- 不要重复输出「每日简报」大标题（页面已有），直接从第一个话题分组开始
- 只输出简报正文 Markdown，不要任何其他说明文字
"""

# ═════════════════════════════════════════════════════════════════════
# 简报选题 → X 帖子（/monitor 一键转帖用，强模型 + X 轨风格/范例）
# ═════════════════════════════════════════════════════════════════════

XPOST_PROMPT = """你是 X 账号 @zynqorw 的内容创作者。把下方简报选题写成一条可直接发布的 X 帖子。

{redline}

【账号风格】以下是账号的写作风格规范，必须严格遵守：
{style}

【优秀范例】以下是账号发布过的范例，学习其语气、结构、开头钩子的写法（只学写法，不抄内容）：
{fewshot}

【简报选题】
{item}

【写作要求】
- 单条帖子（不是 thread），正文 300 字以内（不含末尾链接）
- 用账号的口吻给出「这件事意味着什么、对读者有什么影响」的判断，不做干巴巴的新闻转述
- 信息只能来自【简报选题】：没有的细节不写；绝不编造「我测了/我用了」类亲历声明，转述用「据报道/消息称」
- 结尾单独一行附来源链接（原样使用选题里的链接）
- 只输出帖子正文，不要任何解释或标题行
"""
